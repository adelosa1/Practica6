hosts: ApacheServer3
become: true
vars:
      gitdir: "/tmp/Bookzy
tasks:
      apt: 
          name:tomcat9
          update_cache: yes
      notify: "enable tomcat service"
    copy: 
          src: "{{ girdir }}/target/demo.war
          dest:"/var/lib/tomcat9/webapps/demo.war
          
 handlers:
       service:
            name: tomcat9
            state:started
            enabled:yes
        service:
          name: tomcat9
          state: restarted
        
